export type Book = {
  id: string;
  isbn: string;
  title: string;
  author: string;
  publisher: string;
  category: string;
  reason: string;
  owner: string;
  createdAt: string;
  description: string;
};